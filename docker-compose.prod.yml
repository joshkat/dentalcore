services:
  postgres:
    image: postgres:16-alpine
    container_name: dentalcore-postgres
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: dentalcore-backend
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      FRONTEND_URL: ${FRONTEND_URL}
      CORS_ALLOWED_ORIGINS: ${FRONTEND_URL}
      # Instance default language (en|es) — deployers set this per region.
      DEFAULT_LANGUAGE: ${DEFAULT_LANGUAGE:-en}
      COOKIE_SECURE: "true"
      # the frontend nginx proxies /api and sets X-Forwarded-For; the backend
      # is not directly exposed, so the proxy header can be trusted here
      TRUST_PROXY_HEADERS: "true"
      DOCUMENT_STORAGE_PATH: /data/documents
    volumes:
      - documents:/data/documents
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      target: prod
    container_name: dentalcore-frontend
    ports:
      - "${FRONTEND_PORT:-8090}:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata:
  documents:
